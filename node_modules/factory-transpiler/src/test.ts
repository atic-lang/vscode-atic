import { FactoryTokenizerResult, getTokenitzerResult } from './factory-transpiler';
import { IterableStream } from './iterable-stream';
import { Token } from './tokenizer';

const result: FactoryTokenizerResult = getTokenitzerResult('text', true);
const tokens: IterableStream<Token> = result.tokens;

while (tokens.hasEntriesLeft()) {}
